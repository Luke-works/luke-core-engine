<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- ============================================================= -->
    <!-- Parent: Spring Boot 3.4.4                                     -->
    <!-- ============================================================= -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.13</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <!-- ============================================================= -->
    <!-- Project coordinates                                           -->
    <!-- ============================================================= -->
    <groupId>com.luke</groupId>
    <artifactId>luke-core-engine</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>Luke Core Engine</name>
    <description>CIBSeven Camunda Engine with Spring Boot and PostgreSQL</description>

    <!-- ============================================================= -->
    <!-- Properties                                                    -->
    <!-- ============================================================= -->
    <properties>
        <java.version>21</java.version>
        <cibseven.version>2.1.0</cibseven.version>
        <!-- Scripting language engines (JSR-223) -->
        <groovy.version>4.0.24</groovy.version>
        <graaljs.version>24.1.1</graaljs.version>
        <jython.version>2.7.3</jython.version>
        <!-- Signatures capability (merged from luke-signature-engine, SIG-M) -->
        <pdfbox.version>3.0.3</pdfbox.version>
        <bouncycastle.version>1.78.1</bouncycastle.version>
        <awssdk.version>2.25.60</awssdk.version>
    </properties>

    <!-- ============================================================= -->
    <!-- Dependency Management: CIBSeven BOM                           -->
    <!-- ============================================================= -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.cibseven.bpm</groupId>
                <artifactId>cibseven-bom</artifactId>
                <version>${cibseven.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- AWS SDK BOM — manages the s3 client version (signatures DocumentStore) -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>bom</artifactId>
                <version>${awssdk.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- ============================================================= -->
    <!-- Dependencies                                                  -->
    <!-- ============================================================= -->
    <dependencies>

        <!-- CIBSeven Spring Boot Starters -->
        <dependency>
            <groupId>org.cibseven.bpm.springboot</groupId>
            <artifactId>cibseven-bpm-spring-boot-starter-rest</artifactId>
        </dependency>
        <!-- CIBSeven webapp removed — Luke Core UI replaces it -->

        <!-- CIBSeven Spin JSON Plugin (for Object/JSON variable serialization) -->
        <dependency>
            <groupId>org.cibseven.bpm</groupId>
            <artifactId>cibseven-engine-plugin-spin</artifactId>
        </dependency>
        <dependency>
            <groupId>org.cibseven.spin</groupId>
            <artifactId>cibseven-spin-dataformat-json-jackson</artifactId>
        </dependency>

        <!-- ========================================================= -->
        <!-- CIBSeven Connect (HTTP/SOAP connectors for Service Tasks) -->
        <!--                                                           -->
        <!-- Without these, the Spring Boot starter's @ConditionalOn-  -->
        <!-- Class auto-config for ConnectProcessEnginePlugin never    -->
        <!-- fires, so the BPMN parser installs no connector parse     -->
        <!-- listener and <camunda:connector> elements on Service      -->
        <!-- Tasks are silently ignored (connector never linked).      -->
        <!--                                                           -->
        <!-- engine-plugin-connect → registers the parse listener that -->
        <!--   binds connectors to Service Tasks at deployment time.   -->
        <!-- connect-connectors-all → the HTTP + SOAP connector        -->
        <!--   implementations invoked at runtime.                     -->
        <!-- Versions are managed by the cibseven-bom.                 -->
        <!-- ========================================================= -->
        <dependency>
            <groupId>org.cibseven.bpm</groupId>
            <artifactId>cibseven-engine-plugin-connect</artifactId>
        </dependency>
        <dependency>
            <groupId>org.cibseven.connect</groupId>
            <artifactId>cibseven-connect-connectors-all</artifactId>
        </dependency>

        <!-- ========================================================= -->
        <!-- Scripting language engines (JSR-223)                      -->
        <!-- Discovered automatically by the engine's ScriptEngine     -->
        <!-- manager; ScriptingEnginePlugin enables GraalVM JS host    -->
        <!-- access so scripts can use `execution`, etc.               -->
        <!-- ========================================================= -->

        <!-- Groovy  → script language "groovy" -->
        <dependency>
            <groupId>org.apache.groovy</groupId>
            <artifactId>groovy-jsr223</artifactId>
            <version>${groovy.version}</version>
        </dependency>

        <!-- JavaScript (GraalVM JS) → script language "javascript" / "js".
             Nashorn was removed from the JDK in Java 15, so JS scripting
             requires GraalVM JS on Java 21. -->
        <dependency>
            <groupId>org.graalvm.polyglot</groupId>
            <artifactId>polyglot</artifactId>
            <version>${graaljs.version}</version>
        </dependency>
        <dependency>
            <groupId>org.graalvm.polyglot</groupId>
            <artifactId>js-community</artifactId>
            <version>${graaljs.version}</version>
            <type>pom</type>
        </dependency>
        <dependency>
            <groupId>org.graalvm.js</groupId>
            <artifactId>js-scriptengine</artifactId>
            <version>${graaljs.version}</version>
        </dependency>

        <!-- Python (Jython) → script language "python".
             NOTE: Jython implements Python 2.7, not Python 3. -->
        <dependency>
            <groupId>org.python</groupId>
            <artifactId>jython-standalone</artifactId>
            <version>${jython.version}</version>
        </dependency>

        <!-- Spring Boot Starters -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <!-- JWT verification (NimbusJwtDecoder) for consumer-gateway Bearer tokens
             minted by luke-auth-engine; verified against its published JWKS. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- Merged from capability-engine: bean validation (@Valid on capability controllers) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <!-- Merged from capability-engine: JOSE/JWT (GatewayTokenVerifier). Also pulled
             transitively by oauth2-resource-server above; declared explicitly to match. -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-oauth2-jose</artifactId>
        </dependency>

        <!-- ========================================================= -->
        <!-- Signatures capability (merged from luke-signature-engine) -->
        <!-- Permissive licenses only: PDFBox (Apache-2.0), Bouncy-    -->
        <!-- Castle bcpkix (MIT), AWS SDK s3 (Apache-2.0). NEVER iText -->
        <!-- (AGPL). Visual stamp + PAdES seal + S3-compatible store.  -->
        <!-- ========================================================= -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>${pdfbox.version}</version>
        </dependency>
        <dependency>
            <groupId>org.bouncycastle</groupId>
            <artifactId>bcpkix-jdk18on</artifactId>
            <version>${bouncycastle.version}</version>
        </dependency>
        <dependency>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
        </dependency>

        <!-- Database: H2 (default) + PostgreSQL (optional via postgres profile) -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Flyway: versioned migrations for the custom luke_* tables (#24).
             Active only under the postgres profile (spring.flyway.enabled). The
             postgresql module is required for Flyway 10+ Postgres support. -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
            <scope>provided</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <!-- ============================================================= -->
    <!-- Build                                                         -->
    <!-- ============================================================= -->
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
                <executions>
                    <!-- Generate META-INF/build-info.properties so /actuator/info reports the
                         build time + version — lets us confirm which commit a deployed instance
                         is actually running (previously /actuator/info returned empty {}). -->
                    <execution>
                        <goals>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
